# KBO 조건부 확률 타자 카드 실험실

React, TypeScript, Vite로 만든 수업용 조건부 확률 웹툴의 원본 프로젝트입니다.

```bash
npm install
npm run dev
```

배포용 정적 파일 만들기:

```bash
npm run build
```

빌드 결과는 `../kbo-conditional-probability`에 생성되며 수학 웹툴 모음의 홈 화면에서 바로 연결됩니다.
